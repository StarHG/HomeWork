using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/* 2. ¬раг с дальностью обзора (скал€рное произведение) когда объект попадает в зону, враг окрашиваетс€ в красный свет. скал€рное произведение и рассто€ние
 * 
 * 3. —павн объектов на сферу под правильным углом.
 */
public class Task2Enemy : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
